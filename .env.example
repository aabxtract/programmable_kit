# Copy to .env and fill in. `.env` is gitignored — keep it that way.
# Get a key at https://programmable.market/developers/api-keys
#
# Only required for authenticated calls (launches.getStatus / the get_launch_status
# MCP tool). Every other endpoint works without it.
PROGRAMMABLE_API_KEY=
